import Title from '../ui/Title'
import { Outlet } from 'react-router-dom';

export default function Root() {

  return (
    <>
      <section>
        <Title/>
        
      </section>
    </>
  );
}
